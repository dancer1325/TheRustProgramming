#![allow(dead_code)]
#![allow(unused_imports)]

// #! It's to turn off some warnings thrown by the compiler

// Starting point for all rust programs
// Just possible to have 1 function named "main"
fn main() {

    // println is a macro, not a function
    println!("Hello, world!");
}
