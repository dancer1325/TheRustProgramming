mod ifconditional;   // Import module in the same directory. TODO: Why do we get a code time error?
mod forloop;
mod matchstatement;
mod whileloop;
mod demowithall;

fn main() {
    // ifconditional::demo();
    // forloop::demo();
    // matchstatement::demo();
    // whileloop::demo();
    demowithall::combinationlock();
}


